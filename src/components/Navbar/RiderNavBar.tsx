import React from 'react';
import { IonToolbar, IonTitle } from '@ionic/react';

const RiderNavBar: React.FC = () => (
  <IonToolbar>
    <IonTitle>Rider</IonTitle>
  </IonToolbar>
);

export default RiderNavBar;